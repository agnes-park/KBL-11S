# KBL 클러치 슈터 분석 프로젝트

## 프로젝트 목표
"클러치 슈터는 진짜 존재하는가?"를 KBL play-by-play 데이터로 검증한다.
축구 xG처럼, 경기 상황(점수차·남은시간)과 슛 난이도를 보정해 선수의 클러치 능력을 수치화한다.

핵심 원칙: 최종 질문은 "존재한다/안 한다"가 아니라 **"선수 간 클러치 실력 분산이
통계적으로 0에서 벗어나는가"**이다. "대부분 운"이라는 결과도 유효한 발견이다.

## 파이프라인 (실행 순서)

R 스크립트는 CSV를 **현재 작업 디렉터리 기준**으로 읽고 쓴다.
`data/`에서 실행할 것: 예) `cd data && Rscript ../R/01_preprocess.R`
각 스크립트 상단의 `SEASON_LABEL`로 시즌을 바꾼다.

| 단계 | 스크립트 | 입력 | 출력 |
|------|----------|------|------|
| 0 수집 | `R/00_crawl.R` | KBL API | `*_full_pbp.csv`, `*_schedule.csv`, `*_score_validation.csv` |
| 1 전처리 | `R/01_preprocess.R` | `*_full_pbp.csv` | `*_state_table.csv`, `*_shots.csv`, `*_player_dim.csv` |
| 2 WP모델 | `R/02_wp_model.R` | `*_state_table.csv` | `*_state_wp.csv`, `KBL_wp_model.rds` |
| 3 레버리지 | `R/03_leverage.R` | `*_state_table.csv`, `KBL_wp_model.rds` | `*_shots_leverage.csv` |
| 4 xG | `R/04_xg_score.R` | `*_shots_leverage.csv` | `*_shots_scored.csv` |
| 5 클러치검정 | `R/05_clutch_analysis.R` | `*_shots_scored.csv` | `*_clutch_players.csv` |

수동 수집(선택): `annotation/make_worklist.R` → 워크리스트 생성 →
`annotation/clutch_shot_annotator.html`로 슛 위치·컨테스트 코딩 → 주석 CSV.
규칙은 `annotation/coding_protocol.md`.

## 현재까지의 결과 (2024-25, 한 시즌, coarse xG)

- WP 모델: σ≈0.355, β≈0.217, Brier≈0.176, 캘리브레이션 양호(0.2~0.4 구간 약한 S자 편차)
- 레버리지: 리그 OReb 확률 0.306, 클러치 평균 LI 3.12 vs 비클러치 0.89, 가비지(20+) 0.08
- xG(coarse): 리그 기준선 2점 0.513 / 3점 0.315
- **클러치 검정: τ²<0, 순열검정 p≈0.43 → 한 시즌·coarse 기준으로는 클러치 실력의 증거 없음**
  - 선수당 클러치 슛 중앙값 7개 → 표본이 너무 작아 검출력 부족

## 세 시즌 통합 결과 (완료 — `R/10_multiseason.R`, 리포트 `docs/multiseason_findings.md`)

다음 단계 1(세 시즌 통합)을 구현. 리그 기준선은 풀링, 선수 단위는 선수×시즌.
드라이버: `./run_multiseason.sh` (01을 3시즌 실행 후 10 실행).

- **크로스-시즌 일관성 검증 통과**: 러닝스코어 100/100/99.6%, 2점 .534/.513/.515,
  3점 .334/.315/.327, OReb .316/.306/.303 — 시즌 편차 상식 범위. 226/227은 2025-26에만
  (비득점, 슛/스코어 무영향). → 풀링 타당.
- 풀링 기준선: σ=0.341, β=0.202, OReb=0.309, xG 2점 0.521 / 3점 0.325.
- **존재검정(풀링, 선수 단위)**: τ² 소폭 양수지만 ICC<0.02, skill_sd≈0.04–0.06(슛1개 σ≈0.48
  대비 미미), 순열 p≈0.025–0.05(한계적).
- **시즌 간 지속성(핵심 시금석): year-to-year 가중상관 r≈0.02, p≈0.83 → 지속성 없음.**
  스플릿-하프 신뢰도(SB)≈0.26.
- **결론: 검정력 3배 후에도 반복 가능한 클러치 실력의 증거 없음.** 풀링의 한계적 존재신호는
  시즌 간 지속성 검정을 통과하지 못함(안정적 실력이 아니라 시즌 내 변동·잡음).

### 방법론 수정 (하드윈)
- 원본 `05`의 분산분해는 `between`(슛수 가중 SSB/N)과 `sampling`(비가중 `mean(σ²/nᵢ)`)을
  **다른 스케일**로 비교해 τ²와 순열 p가 어긋난다. 단일 시즌엔 둘 다 null이라 가려졌으나
  **풀링에서 노출**됨. `10`에서는 표준 불균형 일원 랜덤효과 분산성분
  `τ̂²=(MSB−MSW)/n₀`(순열 SSB 검정과 동일 스케일)로 일치시킴. → `05`도 동일하게 고칠 것.
- dplyr 함정: PBP에 컬럼 `s`(초)가 있어 `mutate(season = s)`가 data-masking으로 그 컬럼에
  묶임 → `.env$s`로 고정해야 함(`10`에 반영).

## Clutch Score (5조 문서 지표) 구현 + 실력 검정 (완료 — `R/11_clutch_score.R`, 리포트 `docs/clutch_score_findings.md`)

방중 5조 설계 문서의 Clutch Score를 충실히 구현하고, 문서에 없는 **존재·지속성 검정**을
붙여 결론까지 도출. `10`과의 핵심 차이: 기준선을 **선수 개인 비클러치 성공률**(리그평균 아님)로
쓰고 **자유투 포함**.

- `CS = PAE × TimeW × MarginW × StateChangeW`. PAE = 실제−(개인 비클러치 성공률×슛값).
  TimeW=2−min(t,300)/300, MarginW=2−min(|점수차|,5)/5, StateChange: LeadExt/Chase 1.00 /
  Lead-Taking 1.05 / Game-Tying 1.10 / Go-Ahead 1.20(성공 가정 분류). 개인 기준선은 표본잡음
  완화 위해 시즌·유형 리그평균으로 축소(k=20).
- **존재검정(선수 단위)**: PAE ICC≈0.006, perm p≈0.03 / CS perm p≈0.02 — 한계적.
- **시즌 간 지속성: CS/시도당 r≈0.08(p≈0.40), 평균 PAE r≈0.006(p≈0.96) → 지속성 없음.**
- **양성 대조군(핵심 설득력): 비클러치 FT% year-to-year r≈0.62, p≈0 → 방법이 '진짜 실력'은
  명확히 검출함.** 그럼에도 클러치 지표는 지속성 0.
- **결론: CS는 선수를 잘 서열화하지만 그 서열은 시즌 간 반복되지 않음.** FT%(재현되는 실력)와
  대비되어, 클러치 랭킹 상위는 기회(볼륨)+시즌 내 변동의 산물 = 반복 가능한 클러치 실력 아님.
- 개인 비클러치 기준선은 `10`의 리그 기준선과 **다른 질문**(개인 상승분 vs 리그 대비 가치)을
  보지만, 두 접근 모두 지속성 검정에서 동일 결론(운). → 결론 견고.

## 재구성 3배치 — "실력 없다"를 넘는 능동적 결론 (완료 — `R/12~14`)

`docs/clutch_reframe_findings.md`, `docs/clutch_bounds_findings.md`,
`docs/clutch_heterogeneity_findings.md`. 질문(estimand)을 바꿔 긍정적 결론 도출.

- **배치1 `R/12` (역할·믿음 / 리그효과 / 자유투)**: (A) 과거 클러치 **볼륨**은 미래 볼륨을
  강하게 예측(r=0.69, β=0.69)하나 과거 **효율**은 미래 효율도(r=0.01) 미래 볼륨도(r=0.03)
  예측 못함 → 팀은 효율 근거 없이 같은 선수에게 클러치 역할을 계속 맡김. (B) 클러치엔
  리그 전체 FG%가 유의 하락(.446→.405, p=6e-9), FT%는 평평 → 압박은 구조적. (C) 자유투:
  조직적 초킹 없음(p=.13), 클러치 FT는 개인 평소 FT%로 설명(β=2.28), 별도 클러치요인 없음.
- **배치2 `R/13` (등가·상한 + 승수환산)**: 클러치 실력 SD 95% 상한 0.064(슛당 σ0.48의 1/7),
  지속성 r 95% 상한 0.25. WP단위 레버리지(0.189 WP/클러치슛)로 승수 환산 → **재현 가능한
  클러치 실력의 가치 ≤0.08승/시즌**(SESOI 0.5승). "없다"가 아니라 "있어도 무시가능"을 증명.
- **배치3 `R/14` (국적·팀 이질성)**: 클러치 볼륨은 외국인에 더 집중(41.9% vs 38.3%, p=3e-7),
  FG% 하락은 국적 불문. 외국인 클러치효율 지속성(r=0.54)은 **교란** — 비클러치 지속성이
  더 높음(r=0.65) = '평소 잘 쏘는 선수'(리그기준선 아티팩트), 클러치 특이적 아님. 팀 클러치
  성과 지속성 없음(r=−0.03)이나 최다슈터 점유율(평균 28%)은 안정(r=0.71).
  → **모든 수준에서 재현되는 건 '실력'이 아니라 '역할·구조(누가 던지냐)'.**
- 국적 판정: 한글이름 공백 휴리스틱(외국인 전사명엔 공백, 국내명엔 없음). 42/211 외국인.

### 명성 클러치 선수 케이스 스터디 (`R/16_named_players.R`, `docs/named_clutch_players.md`)
리그가 '클러치'로 부르는 선수들도 이름으로 확인 → **볼륨(역할)은 상위, 효율은 중간 이하**.
대상 선수 26시즌 평균 백분위: 볼륨 76 / 효율 48. 대상만의 시즌간 상관 효율 r=−0.41, 볼륨 r=0.56.
- **이정현**: 클러치 볼륨 백분위 100/99/96(리그 최다급)이나 효율 47/43/34(중간 이하·하락).
- **김선형**: 볼륨 82/96/84 상위, 효율 34/31/41, 평균 PAE 3시즌 모두 음수.
- 오세근·이재도의 최상위 효율 시즌(백분위 93~96)은 8~10시도 소표본 잡음(직전 시즌 하위권).
→ '클러치 명성'=반복되는 성과가 아니라 팀·팬이 부여한 **역할·기대**. (동명이인은 영문명으로 분리)

## 반드시 알아야 할 것 (하드윈 결정·주의사항)

### 이벤트 코드 (a 컬럼, 3자리 문자열)
- 확정: 201 made_2, 202 miss_2, 205 made_3, 206 miss_3, 203 made_ft, 204 miss_ft,
  207 made_2(특수), 209 oreb, 210 dreb, 211 assist, 213 block, 216 foul,
  225 ft_awarded, 101/102 sub in/out, 001/009 period start/end, 003 timeout
- **미확정(레전드 필요)**: 212·214·215·217·218·221·223·224 (턴오버/스틸/파울 서브타입)
- **226·227**: 2025-26 시즌에 새로 등장. 비득점 확정(정상 경기 수천 건에 등장).
  226은 거의 항상 직전과 같은 팀, 227은 반반. 정체 미확정 → 레전드로만 확정 가능.

### 치명적 함정
- **이벤트 순서는 `n` 컬럼**(경기 전체 관통 순번)으로. `time_remaining`은 파생
  이벤트(어시스트 등)에서 부정확하게 찍힘. 정렬키: `(game_id, period, api_row_order)` = `n` 순서와 동치.
- **R의 `ifelse` NA 전파**: `ifelse(t==code, points, 0L)`는 t가 NA인 이벤트
  (period start/end 등)에서 NA를 반환→cumsum이 이후 전부 오염. 반드시
  `ifelse(!is.na(t) & t==code, points, 0L)`. (전처리에서 픽스됨)
- **CSV 재로드 시 코드/팀이 숫자로 읽힘**: `str_pad(sub("\\.0$","",as.character(x)),3,"0")`로 정규화.

### 어시스트 (중요)
- `211`(어시스트)은 **특정 슛에 신뢰성 있게 귀속 불가**. 슛과 인접하지 않고
  (88%가 성공 슛 직후 아님), 같은 팀 비율 50%, `c` 링크 없음, time_remaining 부정확.
- KBL 규칙상 어시스트는 파울 얻은 슛·자유투 실패·속공 드리블 등에도 붙음.
- → `assisted` 컬럼은 **의도적으로 제거**함. xG는 슛값(2/3)만 사용. 레전드 확보 시 재도입.

### 데이터 품질 (감사 완료 — `R/15_data_audit.R`, 리포트 `docs/data_audit_findings.md`)
독립 3소스 삼중검증(일정 API scoreH/A = PBP official = 이벤트 재구성): **809/810(99.9%)**.
이벤트-득점 매핑·팀코드(일정↔PBP)·쿼터구조·슈터결측 모두 0건 통과. 남은 이상치 3종은 국소·무해:
- **S47G01N2 (2025-26)**: 재구성이 공식보다 **홈 −3, 원정 −4** 부족(단순 홈 3점 아님 — 다중 이벤트 누락).
  일정 API=PBP 공식은 일치 → 최종점수 신뢰 가능. 상류 API 이벤트 누락(재수집 미해결). 1/270, 그 경기만 제외/보정.
- **`n` 시퀀스 깨진 경기 2개 (2024-25 S45G01N127, S45G01N95)**: `n`이 전부 0. 그러나 `api_row_order`는
  정상이고 `(game_id,period,api_row_order)` 내부중복 0(정렬 well-defined), 점수 일치 → **파이프라인 무영향**.
  ⚠ 단 `n`으로 직접 정렬 금지(이 2경기만 무너짐). 정렬은 항상 `(game_id,period,api_row_order)`.
- **시간표기 오류 1건 (2025-26 S47G01N24 Q4, s=83)**: 비득점 miss_2 단일 이벤트. 무시 가능.
- OT: 정상 수집. OT경기 수 2023-24=10 / 2024-25=9 / 2025-26=13.

### 모델링 원칙
- WP: 확산 베이스라인 `Φ((margin+β·poss)/(σ·√t))`. OT는 해당 OT 잔여시간으로 근사.
- xG는 **리그 기준선**(선수 개인 평균 아님 — 그러면 made-xg가 0이 됨).
- xG에 **클러치 상황을 넣지 말 것**(클러치 효과가 상쇄됨). 상황 중립 난이도만.
- 클러치 점수 = Σ LI × (기대초과득점). 존재검정은 moe의 선수간 분산분해.

## 다음 단계 (우선순위)

1. ~~**세 시즌 통합**~~ ✅ **완료** (`R/10_multiseason.R`, `docs/multiseason_findings.md`).
   결과: 반복 가능한 클러치 실력의 증거 없음(존재검정 한계적, 지속성 없음). 위 "세 시즌
   통합 결과" 참고.
2. **`05` 방법론 픽스 이식**: `10`에서 고친 랜덤효과 분산성분(`τ̂²=(MSB−MSW)/n₀`,
   순열과 동일 스케일)을 단일 시즌 `05_clutch_analysis.R`에도 반영(현재는 스케일 불일치).
3. **수동 xG 격상**: 클러치 슛 위치·컨테스트를 영상으로 코딩(annotation/ 도구).
   엄격 정의(~600개)부터. 블라인드 코딩·카파 측정 필수. → 지금까지 신호가 없으므로 검정력의
   다음 지렛대는 표본 크기가 아니라 **슛 난이도 보정 품질**.
4. **레전드 확보**: 미확정 코드(226/227, 턴오버/스틸/파울 서브타입) 정체 확정.
   → 어시스트 재도입, 턴오버 분석 확장 가능.

## 플레이오프 수집·분석 (크롤러 준비됨 — `R/00b_crawl_playoffs.R`)

포스트시즌은 "정규시즌 더"와 질적으로 다른 무대(모든 포제션 고압박, out-of-sample). 크롤러는
정규 크롤러(`00`)의 검증된 헬퍼를 재사용하고 스케줄만 포스트시즌용으로 바꿈(정규='정규' 이름 제외,
라운드=seasonCategoryName 보존). 산출물 `KBL_<season>_playoff_full_pbp.csv` 등. 무결성은
`PBP_KIND=playoff Rscript ../R/15_data_audit.R`로 검증(15는 KIND 파라미터화됨).
- ⚠ **수집은 api.kbl.or.kr 접근 가능한 곳에서 실행**(관리형 원격세션은 egress 정책으로 kbl.or.kr
  차단됨 — 로컬 실행). `00`은 `KBL_CRAWL_NOAUTORUN=1`로 자동실행 억제 가능(00b가 헬퍼 재사용).
- 분석 방향(데이터 확보 후): ① RS→PO 이월(정규 성과가 PO 예측? 양성대조군 FG%/FT%),
  ② 리그 PO 압박효과(FG% 하락), ③ 'PO riser' 지속성(과거 PO까지 모아야 검정력↑),
  ④ 시리즈 내 적응 동학(수비 키잉 → 후반 게임 효율 하락). 검정력상 정규시즌 추가보다 유효.

### 플레이오프 데이터 수집·분석 완료 (3시즌, `docs/data_audit_playoff_findings.md`, `R/17`)
- 로컬 수집 완료: 2023-24/24-25/25-26 각 21/22/18경기(PO+챔프전), `round` 컬럼 보존.
  **삼중검증 61/61(100%)**. `01_preprocess`는 `PBP_KIND=playoff`로 PO도 전처리(산출물 `_playoff_` 태그).
  `15_data_audit`도 `PBP_KIND`로 PO 검증, 리포트는 KIND별 별도 파일.
- ⚠ PO 5분 클러치 슛은 시즌당 88~155개(선수당 극소) → 좁은 클러치 검정 검정력 없음.
  → 사용자 방향대로 **'큰 무대(플레이오프 전경기) 퍼포먼스'를 클러치로 재정의**해 분석(`R/17`).

## 큰 무대(플레이오프) 클러치 (완료 — `R/17_clutch_playoff.R`, `docs/clutch_playoff_findings.md`)
클러치를 5분 창이 아니라 **'큰 무대에서의 개인 퍼포먼스'**로 재정의. 지표는 기대초과 성공확률.
- **전이(A)**: 큰무대 성과를 예측하는 건 정규 **전반 실력**(r=0.20, p=0.02)이지 정규 **5분 클러치**가
  아님(r=0.03; 회귀 β=0.00 p=0.79로 전반 통제 후 무의미).
- **riser(B)**: '평소보다 PO에서 올린 정도(부스트)'는 정규 클러치로 예측 0(r=0.01), 정규 전반과
  **음의 상관(r=−0.30)=평균회귀**(상위 riser=정규 하위·소표본, fader=정규 상위 스타의 회귀).
  → '큰 무대 깜짝활약'은 발굴 가능한 능력 아님.
- **반복성(C)**: PO 성과 시즌간 r=0.24(p=0.21, 쌍30) — 검정력 낮아 유의X(과거 PO 모으면↑).
- **결론: 클러치를 '큰 무대 퍼포먼스'로 재정의해도 동일** — 큰 무대에서 재현되는 건 전반적
  실력이지 별개의 '큰 경기 클러치 능력'이 아님. 명성 클러치 선수도 PO 부스트 대부분 0 근처
  (이정현 25-26 PO 204시도 +0.03, 김선형 24-25 141시도 −0.08).

## 환경
- R + 패키지: dplyr, stringr, readr, tidyr. 수집은 httr2, jsonlite, tibble.
- 데이터 3시즌: 2023-24, 2024-25, 2025-26 (각 정규시즌 270경기).
- ⚠ 관리형 원격 세션의 egress 정책: 외부 API(api.kbl.or.kr) 차단될 수 있음 → 수집은 로컬에서.
