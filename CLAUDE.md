# KBL 클러치 슈터 분석 프로젝트

## 프로젝트 목표
"클러치 슈터는 진짜 존재하는가?"를 KBL play-by-play 데이터로 검증한다.
축구 xG처럼, 경기 상황(점수차·남은시간)과 슛 난이도를 보정해 선수의 클러치 능력을 수치화한다.

핵심 원칙: 최종 질문은 "존재한다/안 한다"가 아니라 **"선수 간 클러치 실력 분산이
통계적으로 0에서 벗어나는가"**이다. "대부분 운"이라는 결과도 유효한 발견이다.

## 파이프라인 (실행 순서)

R 스크립트는 CSV를 **현재 작업 디렉터리 기준**으로 읽고 쓴다.
`data/`에서 실행할 것: 예) `cd data && Rscript ../R/01_preprocess.R`
각 스크립트 상단의 `SEASON_LABEL`로 시즌을 바꾼다.

| 단계 | 스크립트 | 입력 | 출력 |
|------|----------|------|------|
| 0 수집 | `R/00_crawl.R` | KBL API | `*_full_pbp.csv`, `*_schedule.csv`, `*_score_validation.csv` |
| 1 전처리 | `R/01_preprocess.R` | `*_full_pbp.csv` | `*_state_table.csv`, `*_shots.csv`, `*_player_dim.csv` |
| 2 WP모델 | `R/02_wp_model.R` | `*_state_table.csv` | `*_state_wp.csv`, `KBL_wp_model.rds` |
| 3 레버리지 | `R/03_leverage.R` | `*_state_table.csv`, `KBL_wp_model.rds` | `*_shots_leverage.csv` |
| 4 xG | `R/04_xg_score.R` | `*_shots_leverage.csv` | `*_shots_scored.csv` |
| 5 클러치검정 | `R/05_clutch_analysis.R` | `*_shots_scored.csv` | `*_clutch_players.csv` |

수동 수집(선택): `annotation/make_worklist.R` → 워크리스트 생성 →
`annotation/clutch_shot_annotator.html`로 슛 위치·컨테스트 코딩 → 주석 CSV.
규칙은 `annotation/coding_protocol.md`.

## 현재까지의 결과 (2024-25, 한 시즌, coarse xG)

- WP 모델: σ≈0.355, β≈0.217, Brier≈0.176, 캘리브레이션 양호(0.2~0.4 구간 약한 S자 편차)
- 레버리지: 리그 OReb 확률 0.306, 클러치 평균 LI 3.12 vs 비클러치 0.89, 가비지(20+) 0.08
- xG(coarse): 리그 기준선 2점 0.513 / 3점 0.315
- **클러치 검정: τ²<0, 순열검정 p≈0.43 → 한 시즌·coarse 기준으로는 클러치 실력의 증거 없음**
  - 선수당 클러치 슛 중앙값 7개 → 표본이 너무 작아 검출력 부족

## 반드시 알아야 할 것 (하드윈 결정·주의사항)

### 이벤트 코드 (a 컬럼, 3자리 문자열)
- 확정: 201 made_2, 202 miss_2, 205 made_3, 206 miss_3, 203 made_ft, 204 miss_ft,
  207 made_2(특수), 209 oreb, 210 dreb, 211 assist, 213 block, 216 foul,
  225 ft_awarded, 101/102 sub in/out, 001/009 period start/end, 003 timeout
- **미확정(레전드 필요)**: 212·214·215·217·218·221·223·224 (턴오버/스틸/파울 서브타입)
- **226·227**: 2025-26 시즌에 새로 등장. 비득점 확정(정상 경기 수천 건에 등장).
  226은 거의 항상 직전과 같은 팀, 227은 반반. 정체 미확정 → 레전드로만 확정 가능.

### 치명적 함정
- **이벤트 순서는 `n` 컬럼**(경기 전체 관통 순번)으로. `time_remaining`은 파생
  이벤트(어시스트 등)에서 부정확하게 찍힘. 정렬키: `(game_id, period, api_row_order)` = `n` 순서와 동치.
- **R의 `ifelse` NA 전파**: `ifelse(t==code, points, 0L)`는 t가 NA인 이벤트
  (period start/end 등)에서 NA를 반환→cumsum이 이후 전부 오염. 반드시
  `ifelse(!is.na(t) & t==code, points, 0L)`. (전처리에서 픽스됨)
- **CSV 재로드 시 코드/팀이 숫자로 읽힘**: `str_pad(sub("\\.0$","",as.character(x)),3,"0")`로 정규화.

### 어시스트 (중요)
- `211`(어시스트)은 **특정 슛에 신뢰성 있게 귀속 불가**. 슛과 인접하지 않고
  (88%가 성공 슛 직후 아님), 같은 팀 비율 50%, `c` 링크 없음, time_remaining 부정확.
- KBL 규칙상 어시스트는 파울 얻은 슛·자유투 실패·속공 드리블 등에도 붙음.
- → `assisted` 컬럼은 **의도적으로 제거**함. xG는 슛값(2/3)만 사용. 레전드 확보 시 재도입.

### 데이터 품질
- 러닝스코어 재구성: 2024-25는 270/270, 2025-26은 269/270 공식 일치.
- **S47G01N2 (2025-26)**: 홈 3점 누락. 재수집 2회에도 안 고쳐짐 = 상류 API 누락.
  1/270이라 전체 무해. 그 경기만 제외하거나 박스스코어로 수동 보정.
- OT: X1(1차 연장)까지 정상 수집(2024-25 9경기).

### 모델링 원칙
- WP: 확산 베이스라인 `Φ((margin+β·poss)/(σ·√t))`. OT는 해당 OT 잔여시간으로 근사.
- xG는 **리그 기준선**(선수 개인 평균 아님 — 그러면 made-xg가 0이 됨).
- xG에 **클러치 상황을 넣지 말 것**(클러치 효과가 상쇄됨). 상황 중립 난이도만.
- 클러치 점수 = Σ LI × (기대초과득점). 존재검정은 moe의 선수간 분산분해.

## 다음 단계 (우선순위)

1. **세 시즌 통합**: 검정력을 높이는 핵심. 단계별로 시즌을 다르게 쓴다.
   - 리그 기준선(σ, xG): 세 시즌 **풀링**
   - 선수 클러치: `선수 × 시즌`을 단위로 유지(시즌 간 지속성이 검정의 핵심)
   - **주의**: 통합 전에 핵심 코드(득점·리바운드)가 세 시즌 동일하게 작동하는지 검증
     (리바운드 same-team 비율, 슛 성공률 상식 체크를 시즌별로).
2. **수동 xG 격상**: 클러치 슛 위치·컨테스트를 영상으로 코딩(annotation/ 도구).
   엄격 정의(~600개)부터. 블라인드 코딩·카파 측정 필수.
3. **레전드 확보**: 미확정 코드(226/227, 턴오버/스틸/파울 서브타입) 정체 확정.
   → 어시스트 재도입, 턴오버 분석 확장 가능.

## 환경
- R + 패키지: dplyr, stringr, readr, tidyr. 수집은 httr2, jsonlite, tibble.
- 데이터 3시즌: 2023-24, 2024-25, 2025-26 (각 정규시즌 270경기).
